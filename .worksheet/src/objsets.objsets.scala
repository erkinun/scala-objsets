package objsets

import objsets._

object objsets {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(95); 
  println("Welcome to the Scala worksheet");$skip(32); 
  
  
	val emptySet = new Empty;System.out.println("""emptySet  : objsets.Empty = """ + $show(emptySet ))}
}
